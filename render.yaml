services:
  - type: web
    name: rgb-dumper-auth
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: cd "Team unity tree test" && python3 auth-server.py
    envVars:
      - key: PORT
        value: 10000
